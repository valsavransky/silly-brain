# Required for the AI 20 Questions game (server-side only — never exposed to the browser)
ANTHROPIC_API_KEY=sk-ant-...

# Optional: port for the Express server (Railway sets PORT automatically)
# PORT=3001
